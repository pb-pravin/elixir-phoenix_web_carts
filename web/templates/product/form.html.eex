<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :prodcut_name, "Prodcut name", class: "control-label" %>
    <%= text_input f, :prodcut_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :price, "Price", class: "control-label" %>
    <%= number_input f, :price, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :stock, "Stock", class: "control-label" %>
    <%= number_input f, :stock, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
